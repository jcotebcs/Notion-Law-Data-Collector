# Railway Deployment Configuration
# This file tells Railway how to deploy the application

[build]
  command = "npm install"

[deploy]
  start = "npm start"

[env]
  NODE_ENV = "production"
  # Add NOTION_API_KEY via Railway dashboard